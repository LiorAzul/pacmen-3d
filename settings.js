// ⚙️ World Settings — הגדרות ויזואליות של העולם
// פתחו את הקובץ הזה בעורך הקוד ולחצו על אייקון ה-Tunable values
// כדי לכוונן כל ערך עם סליידר בזמן אמת.

const worldSettings = {
    // ✨ Bloom — אפקט הזוהר של הסצנה
    bloomEnabled: 1,       // 1 = פעיל, 0 = כבוי
    bloomStrength: 0.41,   // עוצמת הזוהר (0 עד 3)
    bloomRadius: 0.52,     // רדיוס התפשטות הזוהר (0 עד 1)
    bloomThreshold: 0.58,  // סף בהירות — נמוך יותר = יותר אובייקטים זוהרים

    // 🎥 Renderer — הגדרות תמונה כלליות
    exposure: 1.0,         // חשיפה כללית (tone mapping)

    // 🎨 Color grade — דירוג צבע של הפריים המלא
    saturation: 1.0,       // רוויית צבע (0 = שחור-לבן, 1 = רגיל, 2 = מוגזם)
    contrast: 1.0,         // ניגודיות (1 = רגיל)
    brightness: 1.0,       // בהירות (1 = רגיל)

    // 🎞️ אפקטים קולנועיים
    vignette: 0.0,         // החשכת פינות (0 = כבוי, 1 = מקסימום)
    filmGrain: 0.0,        // רעש פילם עדין (0 = כבוי, 1 = מקסימום)

    // 🛡️ מד שכבות ההגנה (זיכרון, אובייקטים, ביצועים) — מוצג ב-Edit Mode
    protectionMeter: 1     // 1 = מציג את המד בעולם, 0 = מוסתר
};

World.applySettings(worldSettings);
